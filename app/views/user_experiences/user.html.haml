- page_title "User"

= standard_flash_box

- content_for :left_sidebar do
  .head
    Weeks
  .body
    %ul.weeks
      %h4 A-Term
      - (1..7).each do |i|
        %li= link_to "Week #{i}", events_path
      %hr
      %h4 B-Term
      - (1..7).each do |i|
        %li= link_to "Week #{i}", events_path
      %hr
      %h4 C-Term
      - (1..7).each do |i|
        %li= link_to "Week #{i}", events_path
      %hr
      %h4 D-Term
      - (1..7).each do |i|
        %li= link_to "Week #{i}", events_path

- page_title! "Launch Pad"

%div.experiences_main{:style => 'width: 860px'}
  %h3 Stuff that's happened
  %ul
    - @user.event_experiences.each do |exp|
      %li.event_row
        .top
          .name
            = exp.event.name
          .traits
            = properties_list_sentence(exp)
        .details
          .choices
            You chose to&hellip;
            %span= exp.choice.option.text if exp.choice
          .results
            As a result&hellip;
            %span= exp.choice.result if exp.choice