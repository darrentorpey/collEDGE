- page_title "Edit User"

.top_nav
  = link_to 'All Users', users_path

= standard_flash_box

- semantic_form_for @user, :html => { :class => 'square user_form', :id => '' } do |form|
  %h3 Edit User
  .lower
    = render :partial => 'form', :locals => { :form => form }

.bottom_nav
  %p
    = link_to "Show", user_path(@user)